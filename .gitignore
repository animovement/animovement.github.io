/.quarto/
**/*.quarto_ipynb

# Quarto render output (published via GitHub Actions, not committed)
/_site/

# renv (library is machine-specific; the lockfile IS committed)
renv/library/
renv/local/
renv/cellar/
renv/staging/

# Logo working files: source montage + archived old logos
# (kept locally, but not committed or published)
assets/logos/archive/
assets/logos/ecosystem-sheet.png

# OS
.DS_Store
